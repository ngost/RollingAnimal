using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ContentsPositionInit : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Transform stage0Transform = GameObject.Find("0").transform;
        //float startX = stage0Transform.position.x;
        //gameObject.transform.position = new Vector3(startX-Screen.width*0.5f, gameObject.transform.position.y, gameObject.transform.position.z);
    }
}
